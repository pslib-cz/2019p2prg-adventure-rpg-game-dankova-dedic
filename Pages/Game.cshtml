@page
@model RPG.Pages.Game
@{ ViewData["Title"] = "Game";
}
<p>@Model.Location.Description</p>

@*@for (int i = 0; i < @Model._place.locations[Model.index].Path.Length; i++)
{
    <a asp-route-to="@Model._place.locations[Model._place.locations[Model.index].Path[i]].ID" asp-handler class="btn btn-secondary">@Model._place.locations[Model._place.locations[Model.index].Path[i]].Way</a>
}*@